document.addEventListener (); {
    const themeForm = document.getElementById('themeform');
    const contentDiv = document.getElementById('content'); 
}

const Theme = [ FirstDayOfFall, Halloween, Thanksgiving ];

const grabChangingTheme = () => {
    const chooseTheme = chooseTheme();
} 


// Apply the selected theme on change
themeForm.addEventListener('change'), () => {
    contentDiv.classList.remove('FirstDayofFall', 'Halloween', 'Thanksgiving');
} 